<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{{ 'DOSTĘP ZABRONIONY!'|trans }}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

        <link rel="stylesheet" href="{{ asset('css/access_denied.css') }}">
        <link rel="icon" type="image/png" sizes="16x16"  href="{{ asset('img/favicon-16x16.png') }}">
    </head>
    <body>
    <h1>403</h1>
    <div class="animate">
        <p>> <span>{{ 'KOD BŁĘDU'|trans }}</span>: "<i>HTTP 403 Forbidden</i>"</p>
        <p>> <span>{{ 'OPIS BŁĘDU'|trans }}</span>: "<i>{{ 'Dostęp zabroniony. Nie masz pozwolenia na dostęp do tej strony'|trans }}</i>"</p>
        <p>> <span>{{ 'BŁĄD PRAWDOPODOBNIE SPOWODOWANY PRZEZ'|trans }}</span>: [<b>{{ 'zabroniony dostęp do wykonywania, zabroniony dostęp do odczytu, zabroniony dostęp do zapisu, wymagany ssl, wymagany ssl 128, adres ip odrzucony, wymagany certyfikat klienta, odmowa dostępu do witryny, zbyt wielu użytkowników, nieprawidłowa konfiguracja, zmiana hasła, odmowa dostępu do mapowania, unieważnienie certyfikatu klienta, katalog odrzucenie listy, przekroczenie licencji dostępu klienta, certyfikat klienta jest niezaufany lub nieprawidłowy, certyfikat klienta wygasł lub nie jest jeszcze ważny, logowanie paszportowe nie powiodło się, odmowa dostępu do źródła, odmowa nieskończonej głębokości, zbyt wiele żądań z tego samego adresu IP klienta'|trans }}</b>...]</p>
        <p>> <span>{{ 'KILKA STRON DO KTÓRYCH MASZ DOSTĘP'|trans }}
            </span>: [<a href="{{ path('admin') }}">{{ 'Strona Główna'|trans }}</a>, <a href="{{ path('bonus_goals') }}">{{ 'Karty celów'|trans }}</a>, <a href="/">{{ 'Struktura organizacji'|trans }}</a>, <a href="{{ path('notifications') }}">{{ 'Powiadomienia'|trans }}</a>...]</p><p>> <span>
                {{ 'MIŁEGO DNIA' }} {{ app.user.firstName|upper }} :-)</span></p>
    </div>
    <script>
        let str = document.getElementsByTagName('div')[0].innerHTML.toString();
        let i = 0;
        document.getElementsByTagName('div')[0].innerHTML = "";

        setTimeout(function() {
            let se = setInterval(function() {
                i++;
                document.getElementsByTagName('div')[0].innerHTML = str.slice(0, i) + "|";
                if (i === str.length) {
                    clearInterval(se);
                    document.getElementsByTagName('div')[0].innerHTML = str;
                }
            }, 10);
        },0);
    </script>
    </body>
</html>